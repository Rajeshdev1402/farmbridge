{
  "name": "frontend",
  "version": "1.0.0",
  "description": "FarmBridge Frontend",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html css js dist/ || true"
  }
}
